// Return type of string is not very useful, so we can use enums to make it more readable:
export enum StorageKeys {
  LikedCharacters = "LikedCharacters",
}

export enum LikedStatus {
  Liked = "heart",
  NotLiked = "heart-o",
}